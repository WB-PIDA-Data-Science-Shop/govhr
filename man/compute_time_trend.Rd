% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_statistics.R
\name{compute_time_trend}
\alias{compute_time_trend}
\title{Compute time trend}
\usage{
compute_time_trend(.data, group, measure_col = NULL)
}
\arguments{
\item{.data}{A data frame containing at least a `ref_date` column.}

\item{group}{Character string naming the grouping column, or `"ref_date"` for
no grouping.}

\item{measure_col}{Character string naming the numeric column to sum, or
`NULL` to count rows.}
}
\value{
A summarized data frame with columns `ref_date`, optionally `group`, and `value`. Value denotes either a sum or headcount (if `measure_col` is `NULL`).
}
\description{
Summarizes data over time by grouping variable, producing a tidy data frame
with `ref_date`, optional group column, and `value`.
}
\details{
When `measure_col` is `NULL`, counts rows per period (headcount). When a
column name is supplied, sums that column per period (wage bill).
}

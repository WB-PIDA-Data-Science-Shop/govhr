% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wagebill.R
\name{compute_wagebill_decomposition}
\alias{compute_wagebill_decomposition}
\title{Decompose wage bill growth into headcount, wage, composition, and
interaction effects}
\usage{
compute_wagebill_decomposition(
  dt,
  group_cols,
  pay_col = NULL,
  period_col,
  periods = NULL,
  id_col = NULL,
  n_col = NULL,
  w_col = NULL
)
}
\arguments{
\item{dt}{data.table, employee-level (or pre-aggregated w/ n_col/w_col)}

\item{group_cols}{grouping columns, e.g. c("grade", "institution")}

\item{pay_col}{individual pay column (ignored if n_col/w_col given)}

\item{period_col}{column identifying the period}

\item{periods}{c(base, comparison); defaults to the two smallest values}

\item{id_col}{optional employee id, for uniqueN() headcount}

\item{n_col, w_col}{optional, if dt is already aggregated to group-period}
}
\value{
list(by_group, summary) -- see Details
}
\description{
Decompose wage bill growth into headcount, wage, composition, and
interaction effects
}
\details{
Group-level identity (exact, no residual):
  \code{delta_W_g = delta_n_g*w0_g + n0_g*delta_w_g + delta_n_g*delta_w_g}
  \itemize{
    \item \code{headcount_effect_grp = delta_n_g * w0_g} -- people gained/lost in group g, valued at last year's pay
    \item \code{wage_effect_grp = n0_g * delta_w_g} -- last year's staff in group g, valued at this year's raise
    \item \code{interaction_effect_grp = delta_n_g * delta_w_g} -- new staff paid at the new rate (joint effect)
  }

Aggregate split, using group shares \code{s_g = n_g/N} to separate "more
people" from "people shifted toward higher/lower-paid groups":
  \itemize{
    \item \code{headcount_effect} -- growth from total headcount change alone
    \item \code{wage_effect} -- growth from pay raises within groups, at last year's workforce mix
    \item \code{composition_effect} -- growth from the mix shifting toward higher/lower-paid groups
    \item \code{interaction_effect} -- leftover 2nd-order cross terms; usually small,
      large only when hiring and raises were concentrated in the same groups
  }
\code{summary$component_sum == summary$actual_delta_w} by construction (validation check).
}

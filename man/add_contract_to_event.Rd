% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retirement.R
\name{add_contract_to_event}
\alias{add_contract_to_event}
\title{Add contract information to event records}
\usage{
add_contract_to_event(
  events,
  contracts,
  keep_vars,
  event_dt = NULL,
  contract_dt = NULL
)
}
\arguments{
\item{events}{A data.table containing personnel event records. Must include the columns
\code{personnel_id} and \code{ref_date}.}

\item{contracts}{A data.table containing contract information, also including \code{personnel_id}
and \code{ref_date}. The contract dataset provides additional attributes describing the personnel's
contractual context on each reference date.}

\item{keep_vars}{A character vector of variable names in \code{contract_dt} to be merged into the
event dataset. These typically describe contract-level attributes such as position, department,
or employment type.}

\item{event_dt}{Deprecated. Use \code{events} instead.}

\item{contract_dt}{Deprecated. Use \code{contracts} instead.}
}
\value{
A data.table identical to \code{event_dt}, but with the specified variables from \code{contract_dt}
joined in by matching on \code{personnel_id} and \code{ref_date}.
}
\description{
This function merges contract information into an event dataset (such as hires, terminations, or transfers)
by matching on \code{personnel_id} and \code{ref_date}. It ensures that selected variables from the contract dataset
are attached to corresponding events without duplicating records.
}
\details{
The function performs a \emph{right join} operation of the \code{contract_dt} onto \code{event_dt}
(via \code{data.table}'s \code{on} syntax). Only unique combinations of \code{personnel_id}, \code{ref_date},
and \code{keep_vars} are retained from the contract dataset prior to the join, preventing
duplicate key matches.

This function is particularly useful when enriching HR event logs with contextual information
about the employee’s contract at the time of each event.
}
\examples{
\dontrun{
library(data.table)

event_dt <- data.table(
  personnel_id = c(1, 2, 3),
  ref_date = as.IDate(c("2020-01-01", "2020-02-01", "2020-03-01")),
  type_event = c("hire", "fire", "hire")
)

contract_dt <- data.table(
  personnel_id = c(1, 2, 3),
  ref_date = as.IDate(c("2020-01-01", "2020-02-01", "2020-03-01")),
  department = c("Finance", "HR", "IT"),
  contract_type = c("permanent", "temporary", "consultant")
)

enriched_events <- add_contract_to_event(
  event_dt,
  contract_dt,
  keep_vars = c("department", "contract_type")
)
}

}
\seealso{
\code{\link[data.table:merge]{data.table::merge()}}, \code{\link[data.table:unique]{data.table::unique()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treatment.R
\name{fix_underage_workers}
\alias{fix_underage_workers}
\title{Flag or remove underage workers}
\usage{
fix_underage_workers(data, min_age = 18, treatment = c("flag", "filter"))
}
\arguments{
\item{data}{A data.frame with \code{birth_date} and \code{ref_date} columns (Date class).}

\item{min_age}{Minimum working age in years (default: 18).}

\item{treatment}{Handling strategy:
\itemize{
\item \code{"flag"}: Add \code{underage_flag} column (TRUE for underage workers)
\item \code{"filter"}: Remove underage worker records
}}
}
\value{
A data.frame with underage workers handled according to \code{treatment}.
}
\description{
Addresses violations of the \code{personnel_minimum_age} rule. Workers below
\code{min_age} (default: 18) are identified using \code{birth_date} and \code{ref_date}.
Age is calculated as \code{difftime(ref_date, birth_date, units = "days") / 365.25}.
}
\examples{
\dontrun{
fix_underage_workers(personnel_df, treatment = "flag")
fix_underage_workers(personnel_df, min_age = 16, treatment = "filter")
}

}
\seealso{
\code{\link{personnel_rules}}, \code{\link{fix_retirement_age}}
}

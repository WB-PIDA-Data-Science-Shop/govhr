% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{compute_fastsummary}
\alias{compute_fastsummary}
\title{Compute Fast Summary Statistics by Group}
\usage{
compute_fastsummary(
  data,
  cols,
  fns = NULL,
  group_cols,
  output = c("long", "wide"),
  tbl = FALSE,
  groups = NULL
)
}
\arguments{
\item{data}{A \code{data.table}, \code{data.frame}, or tibble. The dataset on which to compute the summaries.
If not a \code{data.table}, it will be converted internally for computation.
The result will be returned in the same class as the input (unless \code{tbl = TRUE}).}

\item{cols}{A character vector. Names of the columns to summarize.}

\item{fns}{Optional. Either:
\itemize{
\item \code{NULL} (default): use all default functions defined by
\code{define_fns()}.
\item A character vector of function names matching \code{define_fns()}.
\item A list of functions or formulas, possibly mixed with character
names referring to \code{define_fns()}.
}}

\item{group_cols}{A character vector. Column(s) by which to group the data
before computing the summary statistics.}

\item{output}{Character. Either \code{"long"} (default) or \code{"wide"} to specify
the output format. \code{"long"} returns one row per group per summary
statistic, \code{"wide"} returns one row per group with multiple columns for
each summary statistic.}

\item{tbl}{Logical. If \code{TRUE}, converts the result to a tibble (\code{tibble::as_tibble()}).}

\item{groups}{Deprecated. Use \code{group_cols} instead.}
}
\value{
A dataset containing the summary statistics for the selected columns.
The output will be either long or wide depending on the \code{output} argument.
The returned object will match the class of the input \code{data} (unless \code{tbl = TRUE}).
}
\description{
\code{compute_fastsummary()} computes summary statistics for selected columns
of a dataset, optionally grouped by one or more variables. It allows
the user to specify a set of functions to apply, either from a predefined
set or custom formulas/functions.
}
\details{
The function constructs the summary calls efficiently using \code{bquote()}
and evaluates them within the \code{data.table} environment. This allows for
fast computation even with large datasets. Custom functions can be
supplied as formulas (e.g., \code{~ mean(.x, na.rm = TRUE)}) or as
pre-defined function names from \code{define_fns()}.
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(x = rnorm(100), y = rnorm(100), group = sample(1:2, 100, TRUE))
# Compute mean and sd by group
compute_fastsummary(dt, cols = c("x", "y"), fns = c("mean", "sd"), groups = "group")

# Use a custom function
compute_fastsummary(
  dt,
  cols = "x",
  fns = list(mean = ~mean(.x, na.rm = TRUE)),
  groups = "group",
  output = "long",
  tbl = TRUE
)
}

}
